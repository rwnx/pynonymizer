scripts:
  before: 
    - SELECT * FROM customer LIMIT 1;
  after: 
    - SELECT * FROM customer LIMIT 1;
    
tables:
  film:
    columns:
      description: bs
      title: word
  customer:
    columns:
      first_name: first_name
      email: unique_email