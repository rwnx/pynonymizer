# This is an example of a strategyfile
# See doc/strategyfiles.md for more information
scripts:
    before:
        - DELETE FROM `blah` where x = 4;
    after:
        - SELECT COUNT(1) FROM `students`;
tables:
  accounts:
    columns:
      current_sign_in_ip: ipv4_public
      last_sign_in_ip: ipv4_public
      username: user_name
      email: company_email
  transactions: truncate
  other_important_table: truncate